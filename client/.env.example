# Optional. When unset, the client calls /api and relies on the Vite dev proxy
# (configured in vite.config.ts) to reach the backend at http://localhost:3001.
# Set this to target a deployed backend.
# VITE_API_URL=https://your-backend.example.com
